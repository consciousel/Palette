import { wallet } from "@cityofzion/neon-core";
import { NeoCliBalance, NeoCliClaimable } from "./responses";
export declare function transformBalance({ net, balance, address, }: {
    net: string;
    balance: NeoCliBalance[];
    address: string;
}): wallet.Balance;
export declare function transformClaims({ net, address, claims, }: {
    net: string;
    address: string;
    claims: NeoCliClaimable[];
}): wallet.Claims;
//# sourceMappingURL=transform.d.ts.map